version: 2

sources:
  - name: external_source
    tables:
      - name: aldi
      - name: tesco
      - name: dunnes
      - name: supervalu
    config:
      meta:
        external_location: "s3://ie-supermarket-data/raw/{name}/{{ var('run_date', modules.datetime.date.today().isoformat()) }}/*.jsonl.gz"